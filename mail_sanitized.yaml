user:
    email: YOUR_EMAIL_ADDRESS # 'from' address
    password: YOUR_EMAIL_PASSWORD # Server password. Best to use an app specific
                           # password in case it gets compromised.
